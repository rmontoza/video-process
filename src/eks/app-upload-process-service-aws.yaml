
apiVersion: v1
kind: Service
metadata:
  name: ms-upload-process-service
spec:
  selector:
    app: ms-upload-process-deployment
  ports:
    - protocol: TCP
      port: 80 
      targetPort: 3000
  type: LoadBalancer

